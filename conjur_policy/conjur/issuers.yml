# =============================================================================
# Issuers
# =============================================================================
# Loaded into branch: conjur
#   conjur policy load -b conjur conjur/issuers.yml
#
# Container policy for dynamic-secrets issuer registrations (AWS, Azure, GCP,
# etc.). Individual issuers are registered via the issuer API, not via policy,
# but this branch is required so issuer resources have a parent.
# =============================================================================

- !policy
  id: issuers
  annotations:
    description: Parent policy for dynamic-secrets issuers.
  body: []
