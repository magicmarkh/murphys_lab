# =============================================================================
# Certificate Authenticator
# =============================================================================
# Loaded into branch: conjur
#   conjur policy load -b conjur conjur/authn-cert.yml
#
# Per-CA authn-cert service-ids should be added as nested !policy entries
# under this branch.
# =============================================================================

- !policy
  id: authn-cert
  annotations:
    description: Parent policy for all certificate authenticator service-ids.
  body: []
