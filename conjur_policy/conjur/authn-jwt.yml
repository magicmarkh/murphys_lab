# =============================================================================
# JWT Authenticator
# =============================================================================
# Loaded into branch: conjur
#   conjur policy load -b conjur conjur/authn-jwt.yml
#
# Per-issuer JWT authenticators should be added as nested !policy entries
# under this branch (e.g. GitHub Actions, GitLab CI, Kubernetes SAT, etc.).
# Add a sub-policy for each service-id with its own variables, webservice,
# and consumer group.
# =============================================================================

- !policy
  id: authn-jwt
  annotations:
    description: Parent policy for all JWT authenticator service-ids.
  body: []
